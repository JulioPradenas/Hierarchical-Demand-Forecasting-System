# Streamlit secrets
# This file is for local development only
# In production, use Streamlit Cloud's secret management

# API endpoint
api_url = "http://localhost:8000"

# MLflow settings
mlflow_uri = "file:///tmp/mlruns"

# Data settings
cache_expiry_hours = 24
