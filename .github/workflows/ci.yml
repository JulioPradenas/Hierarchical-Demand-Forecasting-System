name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.x"
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "Hierarchical Demand Forecasting System/uv.lock"

      - name: Install dependencies
        working-directory: Hierarchical Demand Forecasting System
        run: uv sync --frozen --group dev

      - name: Ruff lint
        working-directory: Hierarchical Demand Forecasting System
        run: uv run ruff check src tests

      - name: Ruff format check
        working-directory: Hierarchical Demand Forecasting System
        run: uv run ruff format --check src tests

      - name: Mypy
        working-directory: Hierarchical Demand Forecasting System
        run: uv run mypy src

  test:
    name: Tests (Python 3.11)
    runs-on: ubuntu-latest
    needs: quality

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.x"
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "Hierarchical Demand Forecasting System/uv.lock"

      - name: Install dependencies
        working-directory: Hierarchical Demand Forecasting System
        run: uv sync --frozen --group dev

      - name: Run tests
        working-directory: Hierarchical Demand Forecasting System
        run: uv run pytest -q --tb=short
